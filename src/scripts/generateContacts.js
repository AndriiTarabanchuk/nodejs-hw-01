// import { faker } from '@faker-js/faker';
// import { createFakeContact } from '../utils/createFakeContact';

const generateContacts = async (number) => {
  // return faker.helpers.multiple(createFakeContact, {
  //   count: number,
  // });
};

generateContacts(5);
