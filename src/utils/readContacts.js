import { PATH_DB } from '../constants/contacts.js';
import fs from 'fs/promises';
export const readContacts = async () => {
  // try {
  //   const contacts = await fs.readFile(PATH_DB);
  //   console.log(await contacts.toString());
  // } catch (error) {
  //   console.log(error);
  // }
};
readContacts();
